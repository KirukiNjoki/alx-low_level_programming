#!/bin/bash
#include "main.h"
void main ()
{
	int ch;
	clrscr();
	_putchar(“Print all lowercase  letters followed by a new line :\n”);
	/* print lowercase letters */
	  for (ch= ‘a’; ch<= ‘z’; ch++)
		_putchar(ch);
	  _putchar (‘\n’) ;
	    getch();
}
return = 0;

