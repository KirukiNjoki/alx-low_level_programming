#include "main.h"
/**
 * main - entry point
 *
 * print out _putchar followed by a newline
 *
 * Return: always 0 (Success)
 */
int main (void)
{
	for ch[8] = "_putchar";
	int m;
	for (m = 0; m < 8; m++)
		_putchar(ch[8]);
	_putchar('/n');

	return (0);
}

